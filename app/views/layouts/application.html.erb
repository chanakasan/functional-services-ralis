<!DOCTYPE html>
<html>
  <head>
    <title>FunctionalRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .wrapper {
        padding: 20px 40px;
      }
      .results {
        display: flex;
        justify-content: space-evenly;
      }
    </style>
  </head>

  <body>
    <div class="wrapper">
      <%= yield %>
    </div>
  </body>
</html>
